import React from "react";

export default function Header(){
    const style = {
        marginBottom: "0.5em",
        textAlign:"center"
    }
    
    return (
        <header style={style}>
            Colour Input Displayer
        </header>
    )
}